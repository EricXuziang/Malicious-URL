# Random Forest Model

Implementing Random Forest algorithm to perform a model that can detect malicious URLs with a certain acceptable percentage.


### Prerequisites

Jupyter Notebook


## Running the tests

With the Jupyter Notebook’s functionalities, we can run the code bit by bit by orders. All the libraries imported will be automatically install into Jupyter.


